
import { User, Post, Notification, NotificationType } from '../types/index';

const mockNotifications: Notification[] = [];

// Empty core users for a fresh start
export const CORE_USERS: User[] = [];
